server:
  port: 8082
eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: ${SION_EUREKA_URI:http://localhost:8761/eureka}

springdoc:
  swagger-ui:
    enabled: false # Disabling the swagger-ui
  api-docs:
    enabled: true # Disabling the /v3/api-docs endpoint